"""Moteur local de transcription + diarisation + identification de locuteurs.

Expose une CLI (`python -m transcriber_engine --request req.json`) appelee par le
service .NET. Gele en un seul executable autonome via PyInstaller.
"""

__version__ = "0.1.0"
