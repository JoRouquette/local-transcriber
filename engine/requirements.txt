# Moteur de transcription/diarisation (gele ensuite avec PyInstaller).
#
# IMPORTANT : stack epinglee et testee sur Python 3.11 (whisperx 3.1 / pyannote 3.1).
# Ne pas utiliser Python 3.13 : il force whisperx 3.8 / pyannote 4 (API differentes,
# dependance torchcodec + DLL FFmpeg partagees). FFmpeg (executable) doit etre sur le PATH.
#
# PyTorch s'installe en amont selon la cible (voir README / build-engine.ps1) :
#   CPU      :  pip install torch==2.2.2 torchaudio==2.2.2 --index-url https://download.pytorch.org/whl/cpu
#   GPU CUDA :  pip install torch==2.2.2 torchaudio==2.2.2 --index-url https://download.pytorch.org/whl/cu121

whisperx==3.1.1
pyannote.audio==3.1.1
faster-whisper==1.0.3
av>=12,<13
ctranslate2==4.4.0
transformers>=4.19,<4.41
# nltk 3.9+ embarque un "finder" (inisec) qui bloque les imports depuis le CWD et
# casse `import whisperx` (via l'alignement). 3.8.1 = derniere version sans ce garde-fou.
nltk==3.8.1
python-dotenv>=1.0.1
srt>=3.5.3
numpy>=1.26,<2.0
soundfile>=0.12.1

# Recherche semantique (sidecar d'embeddings) : e5-small multilingue
sentence-transformers==2.7.0

# Packaging
pyinstaller>=6.6.0
