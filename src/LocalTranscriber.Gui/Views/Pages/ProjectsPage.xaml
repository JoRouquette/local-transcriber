<UserControl x:Class="LocalTranscriber.Gui.Views.Pages.ProjectsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Projets" FontSize="20" FontWeight="Medium" Margin="0,0,0,4" />
        <TextBlock Grid.Row="0" Text="" />

        <DockPanel Grid.Row="1" Margin="0,8,0,12">
            <Button DockPanel.Dock="Left" Style="{StaticResource MaterialDesignRaisedButton}"
                    Command="{Binding AddProjectCommand}" Margin="0,0,8,0">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Plus" VerticalAlignment="Center" Margin="0,0,6,0" />
                    <TextBlock Text="Ajouter" VerticalAlignment="Center" />
                </StackPanel>
            </Button>
            <Button DockPanel.Dock="Left" Style="{StaticResource MaterialDesignOutlinedButton}"
                    Command="{Binding RemoveProjectCommand}">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="TrashCanOutline" VerticalAlignment="Center" Margin="0,0,6,0" />
                    <TextBlock Text="Supprimer" VerticalAlignment="Center" />
                </StackPanel>
            </Button>
            <TextBlock Text="Sous-dossiers du dossier surveillé, avec réglages spécifiques."
                       VerticalAlignment="Center" Margin="16,0,0,0"
                       Foreground="{DynamicResource MaterialDesignBodyLight}" />
        </DockPanel>

        <materialDesign:Card Grid.Row="2" materialDesign:ElevationAssist.Elevation="Dp2">
            <DataGrid ItemsSource="{Binding Projects}" SelectedItem="{Binding SelectedProject}"
                      Style="{StaticResource MaterialDesignDataGrid}"
                      AutoGenerateColumns="False" CanUserAddRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Nom" Binding="{Binding Name}" Width="2*" />
                    <DataGridTextColumn Header="Dossier (relatif)" Binding="{Binding RelativePath}" Width="3*" />
                    <DataGridCheckBoxColumn Header="Actif" Binding="{Binding Enabled}" Width="Auto" />
                </DataGrid.Columns>
            </DataGrid>
        </materialDesign:Card>
    </Grid>
</UserControl>
